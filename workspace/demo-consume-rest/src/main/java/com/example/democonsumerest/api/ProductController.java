package com.example.democonsumerest.api;

record ProductDTO(String name, String category, double price) {}

public class ProductController {
    // write an endpoint for GET: /api/products
    // return a list of ProductDTO
    // use ProductService to get the products
    // use CompletableFuture to make the service calls non-blocking
    // use the barrier pattern to wait for all the results
    // map the products to ProductDTO
    // return the list of ProductDTO

    // write an endpoint for GET: /api/products/{id}

}
